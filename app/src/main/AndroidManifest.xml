<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
<!--  Maps api key  AIzaSyATwseaWRzrjwzWOhT9-Gtk-HVz6q0P0NA-->
    <application
        android:name=".FaceCaptureApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FaceCapture"
        tools:targetApi="31">
        <activity
            android:name=".ui.facedetection.FaceDetectionActivity"
            android:exported="false"
            android:label="@string/title_activity_face_detection"
            android:theme="@style/Theme.FaceCapture" />
        <activity
            android:name=".ui.camera.ScannerActivity"
            android:exported="false"
            android:label="@string/title_activity_scanner"
            android:theme="@style/Theme.FaceCapture" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FaceCapture">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>